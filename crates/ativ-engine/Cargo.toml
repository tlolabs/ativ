[package]
name = "ativ-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "ativ-engine"
path = "src/main.rs"

[features]
# Activation is gated on Core qualification and packaged-target verification.
default = []
managed-runtime = []

[dependencies]
ativ-core = { path = "../ativ-core" }
